# Handoff checkpoint — 2026-06-14（v0.1.8-hotfix 替换包 + 艾莎结算页修复）

> **当前唯一权威 checkpoint**（取代 `handoff_2026-06-13.zh-CN.md` 的 Git/发布/待办状态；06-13 的 Phase 1 技术细节仍有效）。
> **v0.1.8 之后流水**：[`CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md`](CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md)
> **hotfix 发布说明**：[`RELEASE_NOTES_v0.1.8-hotfix.zh-CN.md`](RELEASE_NOTES_v0.1.8-hotfix.zh-CN.md)
> **艾莎主策略**（未变）：[`docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md`](docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md)

---

## 0. 一句话现状（2026-06-14 晚）

**v0.1.8-hotfix 已在本地重建并替换**（仍用同一标签名，非 hotfix2）：含 **拉文 R5 全品质精确锁** + **结算页「估价」泄露修复**（艾莎/通用，仅显示）。
**dev 引擎（未 commit / 未打包）**：约束分层 P0（公开均价 soft + exact 路径 avg_cells 惩罚）、Quote Top3 分档 safety、**LayoutDepthPolicy 抽层**（Aisha full + Raven/Sophie/Gabriela sparse early band）、**hero_ref_live_schedule**（分档 combo cap + 分英雄推理门控）。
**均格 soft promote 暂缓**：tier 价 subset 枚举 **实验 deferred**（见 O-v3-193 / audit fixture）；**现行约束策略不变**。
**Git `main` 本地领先 origin 2 commit**；引擎/UI dev 改动在工作树；**本地 tag `v0.1.8-hotfix` @ `69bc9fd`，远程 tag 仍为旧 `4a51598`**（是否 force-push 标签/替换 GitHub release 资产待用户决定）。群友已用夸克发 v0.1.8 公告；hotfix 包用户自行替换上传。

---

## 1. Git / 发布身份

| 项 | 值 |
|---|---|
| 分支 | `main`，**ahead of `origin/main` by 2**（截至本 checkpoint） |
| HEAD | `69bc9fd`（docs: hotfix 发布物）← `b94c474`（fix: 结算页估价泄露）← … |
| 对外基线 | **v0.1.8** @ `a706bd5`（2026-06-13，夸克群公告） |
| **当前 hotfix 包** | **v0.1.8-hotfix** @ **`69bc9fd`**（替换原 `3fc9271`/`4a51598` 包，标签名不变） |
| 构建源 | `b94c474`，`DirtyWorktree: false` |
| 包路径 | `bidking-lab/dist/BidKingHeroRef-v0.1.8-hotfix-{full,public-safe}.zip` |
| SHA256 | 见 `SHA256SUMS-v0.1.8-hotfix.txt`（full `45D46FC1…E77FD`，public-safe `7AD3AE9D…6CFD8`） |
| 远程 tag | `origin/v0.1.8-hotfix` → **`4a51598`（旧）**；本地已移到 `69bc9fd` |

**未 push 的 commit**（需用户确认后 push）：
- `b94c474` fix(hero-ref): settled-view estimate leak
- `69bc9fd` docs(hero-ref): hotfix release notes + checksums

**工作树未提交**（样本归档 + 规则，见 §4）：
- `data/samples/hero_ref/manifest.json`、`README.zh-CN.md`、新 zip
- 工作区根 `AGENTS.md` / `.cursor/rules/engineering-collaboration.mdc`（**不在 bidking-lab git 内**，备份见 `2026_design/backup_2026-06-14_AGENTS_and_rules/`）

---

## 2. 本窗口已完成（2026-06-14）

### 2.1 拉文 R5 全品质扫描（hotfix 第一批，已在群测）
- 缺档锁 **0/0/0**（无红不再 0/1/2 抬价）；已出现档 **精确锁**（3 红 → 3/3/3，不再 3/3/4）。
- 引擎：`_apply_all_item_quality_exact_counts`（`ahmad_ref_engine.py`）。
- 样本：0 红 `2408:…`；3 红 `2407:` data10 / `data 10`。
- 测试：`test_ahmad_ref_engine_public_info.py` 拉文相关 4 条。

### 2.2 结算页「估价」泄露（hotfix 第二批，艾莎群友反馈）
- **现象**：结算三档卡「估价/结算/差值」左侧估价虚高（如 917k vs 结算 552k）；仅中间「结算」为真值。
- **根因**：`_clone_as_pre_settlement_snapshot` 只翻 `phase/truth`，**未清 `ui_contract.constraints`**（精确结算件数）→ 引擎回读抬高。
- **修复**：剥离 `final_*` / inventory / known_value_sum / minimap_grid_items / model_eval / `ui_contract.constraints|minimap`（`ahmad_live_panel_server.py`）。
- **范围**：**仅结算页显示**；live 竞价不受影响。
- **验证**：`2404:1425860640906376` r02_settled 639,131→402,958（= live r02_bidding）；`test_pre_settlement_clone_strips_settlement_grade_evidence`；overlay 209 passed。
- **归档样本**：`HR-20260614-6376` → `data/samples/hero_ref/archive/exports/2026-06-14/`（theme `aisha_settled_estimate_leak`）。

### 2.3 文档 / 规则（工作区）
- `CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md`、`RELEASE_NOTES_v0.1.8-hotfix.zh-CN.md`、`SHA256SUMS-v0.1.8-hotfix.txt` 已更新。
- **AGENTS.md**：§2 注释哲学、§4 合并去重、`.mdc` 瘦身为指针卡（**仅磁盘 + 2026_design 备份，未进 bidking-lab git**）。

### 2.4 引擎约束 / 布局 / 调度（dev，**未 commit**，2026-06-14 晚）

**约束分层 — O-v3-193 原「四项」+ 关联（dev P0）**

| # | 项 | 状态 |
|---|-----|------|
| **1** | 公开 q4/q5/q6 **均价 soft**（件数未锁 → 加权，不硬剪） | ✅ `soft_avg_value_keys` |
| **2** | **combo 级均价软权重**（类似均格 σ） | ✅ `_soft_public_avg_value_log_weight`（prior + exact） |
| **3** | 均价 **wire float 规范化** | ✅ 更早已做；`public_*_avg_value_wire_normalized` |
| **4** | **soft 均格 → 唯一件数 promote 为 derive** | ⏸ **暂缓**（见下） |

**均格 / exact / derive（关联，非四项本体）**

| 项 | 状态 |
|----|------|
| public q4/q5 **均格 soft** | ✅ 既有；exact 补 `_evidence_grid_avg_cells_log_penalties` |
| **derive 硬路径**（均价+均格交集、q5-only 等） | ✅ 保留 |
| prop **`100114` 红均格** | **仍硬**；`100113` 金均格是否进 soft 族 → 观察中 |
| **Top3**：分档 safety 0.90/0.85/0.80 + `intra_quality_value_band_v0` | ✅ |
| registry ↔ 引擎 hard/soft **全表** | 部分；100114 与 registry 仍不一致 |
| Top3 safety **按 hero/轮次再调** | 观察中 |

详见 `OBSERVATIONS_V3.md` **O-v3-193**。

**Quote Top3 微调**
- 分档 safety：P25×0.90 / P50×0.85 / P75×0.80（原统一 0.85）；note `ref_quote_safety_tier_v1`。
- 实现：`layout_depth_policy.quote_safety_multipliers()` → `run_reference_engine`。

**LayoutDepthPolicy 抽层**
- 新模块：`external_references/ahmad_live_reference_lab/src/layout_depth_policy.py`。
- **Aisha**：R1–2 early viewport + R3+ footroom **band**（行为与 Phase 1 C2 一致）。
- **Raven / Sophie / Gabriela**：`sparse_early` — R1–4 仅 early band 扩上界，更低 cap、更高 sparsity boost；**无 R3+ footroom point raise**。
- **Ahmed**：不接入 layout 泛化。
- `prepare_reference_engine_snapshot`：sparse 英雄默认 `audit_aisha_layout_mode=band`（仅 Aisha 仍默认 d1=shadow）。
- 引擎入口：`_apply_layout_depth_hints_from_snapshot()`（替代原 `_apply_aisha_early_*` + `_apply_aisha_layout_*` 双调用）。

**Hero 推理调度**
- `tools/hero_ref_live_schedule.py`：20 英雄分类；`PRIORITY_MANAGED` = aisha/ahmed/raven/wuqilin/sophie/gabriela；combo cap 分档（sparse 1500 … late 12000）；Category C 早轮单技能门控。
- 已接入 `ahmad_live_panel_server.py` scheduled inference 路径。

**验证**：`pytest tests/test_ahmad_ref_engine_public_info.py tests/test_ahmad_ref_engine_avg_value_wire_samples.py tests/test_live_overlay.py` → **392 passed**（dev 工作树）。

**暂缓 / 实验（记录备查，不接入 live）**

| 项 | 状态 | 说明 |
|----|------|------|
| **#4 均格 soft promote** | planning_deferred | `data/fixtures/soft_promote_count_planning.json` |
| **soft 均价 promote**（#4 镜像） | planning_deferred | 同上 fixture §items[1] + risks |
| **tier 价 → subset 枚举** | experimental_deferred | `tier_value_subset.py` + audit fixture |

---

## 3. 艾莎整体优化（仍 live，详见 06-13 handoff + 策略 doc）

摘要（v0.1.8 Phase 1 + dev layout policy，**hotfix 未改主路径；dev 已抽 LayoutDepthPolicy**）：
- 白绿随机揭示 floor-only；split bridge；B1/B2 总格；C2 R3+ band；C1 good_regression；D1 shadow；防守倍数 UI；动态下一步；低品件/格展示。
- **dev**：LayoutDepthPolicy 模块 + sparse 英雄 early band；Quote 分档 safety。
- **产品口径**：R1–R2 防守价，R3+ 逐步正式估价；白蓝低/抖 = count_prior 早轮特性（非本次 hotfix 范围）。

---

## 3.5 产品方向（2026-06-14 更新）

**下一对外版：v0.1.9** — 聚焦艾莎、计算效率、UI（默认仍不频繁打包，合入后再发）。

| 进 v0.1.9 | 不进 v0.1.9（下次计划） |
|---|---|
| 艾莎计算器 R3+ 才算、指标收窄、卡顿/效率 | **WinDivert 底层更换** |
| UI：版本号、紫金件格、对局体验优化 | |
| 调查：R1→R2 跳水、总格卡结尾、拉文 R5 闪退 | |
| 计划：隐藏最高出价者名字 | |

**仍留 v0.2.0**：count_prior 中心标定（架构级，勿与 0.1.9 混做）。

---

## 4. 待办 / 调查中（勿丢）

| 优先级 | 项 | 状态 |
|---|---|---|
| P0 | **push `main`** | **已做** @ `98a2789`（2026-06-14） |
| P0 | **是否 force-push tag / 更新 GitHub release 资产** | 用户未确认 |
| P0 | **提交样本归档**（manifest + zip + README） | 本地已做，未 commit |
| P1 | 艾莎 R1≈30W→R2≈11W 跳水 | **已审计** — session `2404:1425860640559959`；**未复现 30→11**（R1 0 combo，R2+ ~48.6W count_prior）；见 **O-v3-194** + `data/samples/hero_ref/investigations/aisha_r1_r2_dive_559959/` |
| P1 | 开局给总格数「卡到结尾」 | 群友反馈，未着手 |
| P1 | **UI 版本号**（标题旁 `v0.1.9`） | **已做** @ dev |
| P1 | **紫金件数+格子**（top3 锁 → `紫N/M`） | **已做** @ dev |
| P2 | 拉文 R5 UI 闪退（2410 session） | 待 R5 当下导出 |
| **v0.1.9** | **艾莎计算器**：R3+ 才算；指标=格/件/红/金；不算价差；效率 | **已做** @ `ceb6300` — R1–R2 live 不跑引擎；手动 R1–R2 跳过 60k |
| **v0.1.9** | **live combo cap / UI 不卡** | schedule **8k/12k**；perf audit p50 R3 121ms/R5 17ms；outlier ~11s 需早退 — O-v3-194 |
| **v0.1.9** | **隐藏最高出价者名字** | **已做**：首尾留字、中间 `*`（「最高」行） |
| **dev** | O-v3-193 **#1/#2** 均价 soft + combo 权重 | **已做** @ dev |
| **dev** | O-v3-193 **#3** wire float 规范化 | **已做**（P0 前） |
| **dev** | exact avg_cells 高斯 + Quote 分档 safety | **已做** @ dev |
| **dev** | **LayoutDepthPolicy + hero schedule** | **已做** @ dev，见 §2.4 |
| **暂缓** | O-v3-193 **#4** + soft 均价 promote | planning_deferred | `data/fixtures/soft_promote_count_planning.json` |
| **experimental_deferred** | tier 价 subset 枚举 | audit 完成；价表未对齐 |
| **观察** | prop 100113/100114 均格 soft 族；Top3 safety 按 hero 再调；registry 全表对齐 | 待样本 |
| **下次计划** | **WinDivert 底层更换** | 明确不进 0.1.9 |
| v0.2.0 | count_prior 中心标定（艾莎早轮偏保守） | 架构级，勿轻改 |

---

## 5. 新窗口必读顺序

1. **本文件**（Git/发布/本窗口 delta）
2. [`CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md`](CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md)（逐条变更 + 调查中）
3. [`handoff_2026-06-13.zh-CN.md`](handoff_2026-06-13.zh-CN.md)（Phase 1 技术细节、批测结论、红线）
4. [`docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md`](docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md)（路线图）
5. [`handoff_prompt_next_window.md`](handoff_prompt_next_window.md)（复制给新会话的短 prompt）

## 6. 验证命令（改引擎/UI 后）

```powershell
cd c:\xiangmuyunxing\biancheng\2026\bidking-lab
python -m pytest tests/test_live_overlay.py -k "pre_settlement_clone" -q
python -m pytest tests/test_ahmad_ref_engine_public_info.py -k "raven or all_item_quality or layout or safety_tier" -q
# 艾莎 Phase 1 + layout policy 回归
python -m pytest tests/test_ahmad_ref_engine_public_info.py tests/test_ahmad_ref_engine_avg_value_wire_samples.py tests/test_live_overlay.py -q
```

## 7. 红线（照旧）

- 未经用户明确要求：**不 commit / push / 打包**（hotfix 已例外完成一次）。
- 不在 14 exact-total / 173 curated 上扫权重。
- count_prior 中心标定 = v0.2.0，勿轻改。
- C2 target、D1 apply 已否决，勿 live。
