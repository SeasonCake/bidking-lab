# BidKing Lab 总交接 - 2026-06-10

本交接用于把当前长窗口的主线 v3 与 Hero Ref 支线同时收口，供新窗口直接接续。  
更细的支线实录、反馈记录和 release 细节见 `external_references/ahmad_live_reference_lab/EXECUTION_NOTES_2026-06-10.zh-CN.md`。

## 0. 新窗口先读

1. `C:\xiangmuyunxing\biancheng\2026\AGENTS.md`
2. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\PROGRESS_V3.md`
3. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\DECISIONS_V3.md`
4. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\OBSERVATIONS_V3.md`
5. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\docs\PROJECT_STRUCTURE_V3.zh-CN.md`
6. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\README.zh-CN.md`
7. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\HANDOFF_2026-06-10.zh-CN.md`
8. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\EXECUTION_NOTES_2026-06-10.zh-CN.md`

先确认：

```powershell
cd C:\xiangmuyunxing\biancheng\2026\bidking-lab
git status --short
git log -3 --oneline
```

当前可见最新提交是：

```text
47624a2 Fix Hero Ref fast raw capture path
cbd74a5 Improve Hero Ref live waiting diagnostics
c9d3acc Keep Hero Ref UI open when monitor exits early
```

## 1. 当前总判断

- 主线 v3 仍是 `not_ready`，继续 shadow-only / audit-only，不能恢复 formal/value sampler 的正式接入。
- Hero Ref 支线已经到收尾与发布整理阶段，优先继续这个支线。
- 另一个窗口里已经有不少 v3 改动，不要回滚、不要清理、不要把它们当成当前窗口的目标。

## 2. 当前已经确认的关键事实

- `0x002D` settlement payload 里有完整 inventory，不是缺块。
- 4406 这局的 24 件结算 inventory 已能完整解析，小地图 `layout_complete=true`。
- 这局里游戏侧显示的 `战利品价格 728,211` 与本地结算值 `477,562` 不一致，更像价格表 / 活动版本 / 表漂移，不像 parser 把 settlement block 丢了。
- 2401/2406/2410 相关主线 blocker 仍然优先按 table/source semantics 解释，不要退回到“先看是不是缺块”这一条。
- Hero Ref 侧的均格证据口径已经收紧：
  - `avg * count` 要接近整数格；
  - 不再用一位小数容差把 `1.8` / `1.80` 混成同一个 hard candidate；
  - `0` 均格是合法证据；
  - 件数证据和格数证据分开看。

## 3. 这次对话里保留下来的实战反馈

这部分更细的原始反馈、我的逐条回答、以及能复用的检查结果，已集中写入 Hero Ref 支线 notes。  
这里先保留结论级摘要，避免新窗口重新跑一遍同样的判断：

- 朋友反馈的核心不是“UI 好不好看”这么简单，而是：
  - 公开/抽检能看到红轮廓，但读不出具体物品名；
  - 金均格、紫均格、白绿均格等输入需要按精确数值处理；
  - 三档估价不能都被同一个红件数单点支配；
  - 手填区域太隐蔽、布局不顺；
  - 开局 `?` 英雄名通常是本机 `player_id` 绑定晚，不是英雄表缺失；
  - `post_game_live` / 日志 / release 包要明确；
  - `safe` 包和 `full` 包要分清。
- 我这边的对应回答已经逐条落到 Hero Ref notes 里，核心方向是：
  - count / cells / value 分层；
  - 公共轮廓分成硬约束和软线索；
  - 手填值保留用户精度，UI 显示可以 compact，但 engine 不要偷换语义；
  - 结算态和待机态要能自动清空；
  - full 包是给别人直接跑的，safe 包是不给原表时的导入包。

## 4. 当前主线 v3 该怎么继续

如果新窗口继续主线 v3，不要回到大规模调参。按当前状态应继续：

- 维持 `shadow-only` / `audit-only`。
- 继续收口 source/profile guard、table/version drift、activity overlay、CSE/SCP 以及 q6 sampler 的 holdout。
- 不把 Hero Ref 的显示口径误当成主线 truth。
- 不恢复 formal/value sampler 的正式出价接入，不 archive v2。

## 5. 当前 Hero Ref 支线该怎么继续

Hero Ref 已经接近实战可发布状态，下一窗口优先做这几件事：

- 验证 full 包是否能在干净解压后直接运行。
- 确认 logs 路径固定为 `data\logs\live\`，并在说明里写清楚。
- 检查 UI 关闭时 monitor 是否联动关闭，除非显式保留。
- 用最新真实样本再跑一次 Ahmad / Victor / Aisha smoke，确认：
  - 英雄识别；
  - 手填清理；
  - 轮次切换；
  - minimap / detail / hover；
  - 结算态与旧局态切换；
  - `1.8` / `1.80` 这种显示不会把数值语义弄乱。
- 若继续改 UI，只做小修，不再扩成 v3 级别复杂度。

## 6. 建议的新窗口 prompt

优先建议新窗口默认接 Hero Ref 支线；如果用户明确切回主线 v3，再按主线继续。

```text
项目目录：C:\xiangmuyunxing\biancheng\2026\bidking-lab

请先读取：
1. C:\xiangmuyunxing\biancheng\2026\AGENTS.md
2. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\README.zh-CN.md
3. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\HANDOFF_2026-06-10.zh-CN.md
4. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\EXECUTION_NOTES_2026-06-10.zh-CN.md
5. C:\xiangmuyunxing\biancheng\2026\bidking-lab\handoff_2026-06-10.zh-CN.md
6. C:\xiangmuyunxing\biancheng\2026\bidking-lab\PROGRESS_V3.md
7. C:\xiangmuyunxing\biancheng\2026\bidking-lab\DECISIONS_V3.md
8. C:\xiangmuyunxing\biancheng\2026\bidking-lab\OBSERVATIONS_V3.md

先确认：
git status --short
git log -3 --oneline

当前默认任务先继续 Hero Ref 支线收尾，不推进主线 v3 promotion，不回滚另一个窗口的改动。

Hero Ref 目标：
把 `BidKingHeroRef` 做到能稳定发布和分享的可实战小工具，继续验证 compact Tk UI、manual fallback、live monitor、Ahmad/Victor/Aisha 的 ref_v0 路线。
重点检查：
1. full 包是否能在干净解压后直接运行；
2. `safe` / `full` 包边界是否写清楚；
3. logs 是否落在 `data\\logs\\live\\`；
4. UI 关闭时 monitor 是否联动关闭；
5. 英雄名、手填、轮次切换、minimap、结算态是否稳定；
6. `1.8` / `1.80` 这类浮点显示是否只影响展示，不影响引擎语义。

如果用户明确切回主线 v3，再回到 v3 的 shadow-only / audit-only 路线；不要把 Hero Ref 的显示口径当成主线 truth。
```

## 7. 2026-06-11 补充

- Hero Ref 小版本已被用户实测认为达到可发布小版本要求，当前新增反馈先按低风险 UI 收口处理。
- 结算偶发不完全仍待用户提供新的 `data` 路径 / 导出包后回查；没有样本前不要直接归因 missing settlement block。
- 已落地 / 已回退：
  - 候选值显示已回退到从小到大，和主界面推荐价口径保持一致；
  - Tk overlay 增加可选 taskbar 模式：源码 / live / portable 启动均支持 `-ShowTaskbar`，底层 UI 参数为 `--show-taskbar`；默认仍保持悬浮 overlay。
  - 包模板新增两个推荐启动入口：`管理员启动HeroRef_悬浮窗.bat` / `Start-HeroRef.bat` 和 `管理员启动HeroRef_任务栏窗口.bat` / `Start-HeroRef-Taskbar.bat`；双击会自动申请管理员权限；新包不再包含 `右键管理员运行_启动HeroRef.bat`，避免把说明式文件名当作主入口。
