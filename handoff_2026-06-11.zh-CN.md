# BidKing Lab 总交接 - 2026-06-11

这个文件是给新窗口直接接手用的。Hero Ref 支线是当前默认优先项；主线 v3 仍然只保留 shadow-only / audit-only，不把 Hero Ref 的显示口径当成主线 truth。

## 0. 新窗口先读

1. `C:\xiangmuyunxing\biancheng\2026\AGENTS.md`
2. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\handoff_2026-06-10.zh-CN.md`
3. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\HANDOFF_2026-06-10.zh-CN.md`
4. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\EXECUTION_NOTES_2026-06-10.zh-CN.md`
5. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\README.zh-CN.md`
6. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\PROGRESS_V3.md`
7. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\DECISIONS_V3.md`
8. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\OBSERVATIONS_V3.md`

可选参考：

- Codex deeplink：`codex://threads/019eaf16-a74a-76f0-8488-804e14d93019`
- 该 deeplink 仅供参考，如需详细信息或者核对内容可以深入查看。

先确认：

```powershell
cd C:\xiangmuyunxing\biancheng\2026\bidking-lab
git status --short
git log -3 --oneline
```

## 1. 当前结论

- Hero Ref 当前状态已经过一轮收口，目标是稳定可发布、可分享的小工具。
- 新提交已经把启动入口统一成明确的管理员命名，避免朋友误点说明式文件名。
- `Start-HeroRef.ps1` 仍然保留自动提权逻辑：非管理员时会 `Start-Process -Verb RunAs`，所以双击入口会自动申请 UAC。
- `-ShowTaskbar` / `--show-taskbar` 仍然保留，taskbar 窗口和悬浮 overlay 都能走同一条启动链。
- 诊断导出 manifest 里已记录启动方式、诊断 profile、raw tables 情况和 public-safe 状态。

## 2. 已确认的映射 / 架构

按 AGENTS 要求，这里只记已经核实过的 source -> transform -> output 链路：

- live monitor -> `latest_snapshot.json` / logs -> Tk overlay render -> 用户看到的建议与状态。
- manual inputs -> `apply_manual_inputs()` / 手填桥接 -> snapshot / summary -> 右侧建议与结算态。
- export diagnostics -> manifest / snapshot / raw jsonl -> 可回传诊断包。
- `data\logs\live\` 仍是默认运行日志落点。
- 结算态/手填态/实时态的切换，当前验证结果没有发现新的结构性错误；之前提到的“偶发结算不完全”仍然保留为样本回查项，不先当成 missing settlement block。

## 3. 已验证的结果

- 最新代码提交：以 `git log -1 --oneline` 为准；本交接随 2026-06-11 Hero Ref / 611 / data4 checkpoint 一起归档。
- 工作树状态：接手时先跑 `git status --short` 复核；本 checkpoint 目标是提交后 clean。
- 回归测试：`C:\Python313\python.exe -m pytest -q` -> `1399 passed, 25 skipped`。
- clean unzip smoke：
  - 包内只保留这四个启动入口：
    - `管理员启动HeroRef_悬浮窗.bat`
    - `管理员启动HeroRef_任务栏窗口.bat`
    - `Start-HeroRef.bat`
    - `Start-HeroRef-Taskbar.bat`
  - 旧中文入口数量为 `0`
- 便携包 manifest 已确认：
  - `SourceCommit=e872c3a`
  - `DirtyWorktree=false`
  - `PublicSafe=False`
  - `IncludesRawTables=True`
  - `RequiresExternalPython=False`
- monitor exe `--help` 返回 `0`。

已生成的 smoke 包：

- `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\dist\_smoke_BidKingHeroRef-e872c3a-launchcheck.zip`
- SHA256：`F1FD3BA51F72BEA02DB03AF4247741B96201EBB4A67BC04442A186BEF0C1A8E4`

## 4. 已经闭环的内容

- 启动入口命名已统一，说明文件和包内 manifest 已同步。
- 默认双击自动提权的说明已写清。
- taskbar 可选模式已保留。
- 便携包能 clean unzip，且包内不依赖外部 Python。
- 诊断包 manifest 已能记录启动模式、profile 和 raw tables 边界。

## 5. 仍需继续盯住的内容

1. 偶发结算不完整或结算展示不完整。
   - 2026-06-11 已补样本索引，不再默认认为没有样本。
   - 优先读取：`docs\hero_ref_settlement_sample_index_2026-06-11.zh-CN.md`。
   - 可复查 raw 根目录：
     - `data\logs\live_2026.06.10_ahmed\live\raw\archive\reset`
     - `C:\Users\shenc\Desktop\recordings\data\logs\live\raw\archive\reset`
   - 已知手动导出包：
     - `C:\Users\shenc\Desktop\recordings\data\logs\live\exports\HeroRefDiag-20260611-021539-4521_1402770788450965.zip`
   - 回查优先顺序：价格表 / 活动版本 / 外部表漂移 -> settlement truth -> UI 展示链路。
2. 如果新样本来了，再按样本核对：
   - 红品下界约束；
   - 均价 / 总价 / 均格是否被正确利用；
   - manual / live 切换是否保留输入且不覆盖用户内容。
3. 主线 v3 继续只保留 shadow-only / audit-only，不把 Hero Ref 口径回写成主线 truth。

## 5.1 2026-06-11 历史 dirty smoke checkpoint

这段是 `e872c3a` clean checkpoint 之后、归档提交之前的继续工作。以下包是当时重新打包后的 dirty smoke 结果，仅作历史排查参考；当前代码状态以最新 git commit 为准。

- settlement raw 批量复放：
  - 已索引：`docs\hero_ref_settlement_sample_index_2026-06-11.zh-CN.md`
  - 54 个含 settlement frame 的 raw WinDivert artifact：`run_reference_engine` 全部 `ok`，`problem_count=0`，`hard_conflict=0`
  - 同一批进入 `summarize_snapshot`：`status=ok=54`、`reference.source=settlement=54`、`ahmed_ref.status=ok=54`
- 新 dirty 包：
  - `external_references\ahmad_live_reference_lab\dist\BidKingHeroRef-v0.1.3-20260611-dirty-portable.zip`
    - SHA256：`CF8B98552025A2721BE4F3311D67A4630FAE05F1A37B09C6060A95E668C85D5A`
    - full/portable，带 raw tables，`RequiresExternalPython=False`
  - `external_references\ahmad_live_reference_lab\dist\BidKingHeroRef-v0.1.3-20260611-dirty-public-safe.zip`
    - SHA256：`530CD1C043F783C78FA2A640EDA518F66B5E2A5B4FBA44DCB63800B8DEBEA8FA`
    - public-safe，不带 raw tables，包含 `PUT_TABLES_HERE.txt`，`RequiresExternalPython=False`
- clean unzip smoke：
  - 两个包均有 UI exe、monitor exe、四个明确启动入口、`Start-HeroRef.ps1`、`data\logs\live`
  - 旧入口 `右键管理员运行_启动HeroRef.bat` 数量为 0
  - UI exe `--help` exit 0；monitor exe `--help` exit 0
- 回归：
  - focused suite：`329 passed, 25 skipped`
  - py_compile：`ahmad_ref_engine.py`、`ahmad_tk_overlay.py`、`ahmad_live_panel_server.py` 通过
- 0611 当前掉落目录复核：
  - `C:\Users\shenc\Downloads\data\raw\tables` 与仓库 `data\raw\tables` 逐文件 hash 一致，`fileVersion=303`；
  - 目前这份 0611 快照没有额外的 `Activity/Item/Drop` 差异可吸收，`土豆服务器` / 世界杯活动名暂时只能记为官方公告信息，不当作本地 raw table 已落地事实；
  - 后续如果拿到新的 `StreamingAssets`，再重新跑 `copy_game_tables.ps1` 和 `build_processed_data.py`，然后再发布适应版。

剩余风险：dirty 包不是 clean tag；未在第二台无 Python 机器实机启动 WinDivert 抓包；金额差价若复现，优先查价格表 / 活动版本 / 外部表漂移。

## 5.2 2026-06-11 后续小修 checkpoint

用户后续明确要求：不要再自行打包；只有用户明确说“打包/发布”时才执行 build/zip。未来包名只保留版本号和包类型，不要再把日期、commit 或 dirty 放进文件名。

- mini UI：
  - `导出` 已从展开后的小地图 header 移到 mini 常驻控制行，群友不用展开详情也能导出诊断包；
  - `地图` 按钮保留 hover 右侧预览和点击固定/取消固定，只移除多余的按钮提示文案；
  - 小地图画布本身和固定小地图里的 hover/tooltip 逻辑未改。
- 包名规则：
  - `external_references\ahmad_live_reference_lab\build_hero_ref_portable.ps1` 新增 `-Version`；
  - 默认 full 目录：`dist\BidKingHeroRef-v0.1.4-full`；
  - 默认 public-safe 目录：`dist\BidKingHeroRef-v0.1.4-public-safe`；
  - `-Version` / `-OutputDir` 若包含日期块或 dirty 标记会直接拒绝；
  - 本 checkpoint 没有重新打包。
- 0611 新内容：
  - 已从游戏原始 `C:\xiangmuyunxing\steamapps\common\BidKing\BidKing_Data\StreamingAssets` 同步当前工作区 raw tables；
  - 当前 `data\raw\fileVersion=308`，`Item.txt/BidMap.txt/Activity.txt/Map.txt/Condition.txt` 与旧快照有差异；
  - `data\processed\items.json` 已重建为 1207 items，包含 `土豆服务器`、世界杯四件限时红物品等 0611 新增物品；
  - `data\processed\items_droppable.json` 已重建为 587 items，口径是 map-reachable physical loot；
  - `Drop.txt` 本身仍未新增 2521/4521 活动 Drop pool，世界杯活动额外掉落权重/source overlay 继续保持 shadow-only / audit-only，不能当作正式 prior。
  - `120006-120009` 四个 q0/cells0/value0 的生肖/称号类 item 只在全量 Drop 审计中出现，来自 `Drop` pool `1001`，但 map_reachable=false 且非物理藏品；当前不进入 `items_droppable`，不参与 MC 概率分布。
  - `1012005` 足球有本地 Drop 权重且 map_reachable=true，仍保留在 `items_droppable`；世界杯红物品和 `土豆服务器` 只有价格/格数可查，尚无本地 Drop 概率。
  - 原始 `Language.txt` 的 `activity_des_10007` 只给地图掉落描述，不给概率；描述中直接提到“退钱”牌、决赛指定用球和高阶场景 4 件限时藏品，未直接提到 `土豆服务器`。
  - 已进一步解码 `dll\Scripts.dll.bytes`：4 字节 XOR key `ryrs` 还原后，`GameServerDemo.Utils.DoDrop` IL 显示正式掉落流程只消费 `Table_Drop.items_list` / `weight_type`，未发现这五个新物品的硬编码权重或活动 overlay 权重。
  - 新增 `data\processed\activity_drop_shadow_prior.json` 和 `scripts\build_activity_shadow_prior.py`：只作为 audit/replay 的拟合参考，`status=shadow_only_not_formal_prior`，不得合入正式 `items_droppable`；shadow JSON 当前是 ignored generated artifact，若要提交需显式 `git add -f`。
  - 当前 shadow leaf weight 估计：`1016007` 约 2721、`1036006` 约 175、`1036007` 约 2640、`1036008` 约 1426、`1076007` 约 2423；其中 `1076007` 不在活动文案边内。
  - shadow JSON 已补 `impact_guard`：正式使用/爆率验证均为 false；高阶活动场景 shadow 新物品加权均值约 938866，最高价值奖杯权重占比约 2.51%，但低置信项仍要求保持 read-only。
  - 新增测试确认上述五个 fitted target 不在 `items_droppable.json`，也不在逐地图 `basic_mc.flatten_pool()` 的正式 map prior；`1012005` 足球保留为正式 prior 正例。
- 0611 兼容补丁：
  - `BidMap` 空分类 fallback 扩到 `2501-2530 -> 105`、`4501-4530 -> 305`，避免 v308 活动图读表失败导致 monitor 不出结果；
  - `basic_mc.flatten_pool` 只纳入 quality>0、value>0、占格>0 的物理藏品；
  - `build_processed_data.py` 从地图入口递归 Drop 树生成 `items_droppable.json`，避免非地图可达/非物理编码污染推理 prior；
  - `copy_game_tables.ps1` 同步列表补入 `Tables\Language.txt`，方便后续直接在 `data\raw\tables\Language.txt` 查活动描述；
  - `build_activity_shadow_prior.py` 生成独立 shadow-only 活动拟合 prior，不接正式推荐；
  - 中文管理员 BAT 启动时会提示无 UAC/等待对局包时右键“以管理员身份运行”；
  - mini `导出` hover 明确写异常/卡住/结算不对时点击，生成诊断 zip，发群里作为 log 排查；
  - `tools\ahmad_tk_overlay.py` 新增 `data\logs\live\hero_ref_ui_runtime_status.json` 覆盖写状态文件，自动记录 UI 最近一次 refresh、snapshot signature/age、summary/manual worker、capture wait_state 等；诊断 zip 会带上该文件，便于排查少数设备“UI 开着但不刷新”。
  - 诊断 zip 也会带上 `monitor.stdout.log` / `monitor.stderr.log`；即使没有 `latest_snapshot.json`，mini 常驻 `导出` 也会生成 `no_snapshot` 诊断包，专门覆盖 `no_capture_status` / monitor 未常驻场景。
  - 打开手填面板时，如果当前没有手填内容，会自动填入当前 live context 的英雄/地图/总件等；已有手填内容不覆盖。
  - `下一步` 在红件已锁但红格未知时改成 `优先补总格/全均格`，不再把 mini 文案导向“红均格优先”。
- v0.1.4 package artifacts:
  - `external_references\ahmad_live_reference_lab\dist\BidKingHeroRef-v0.1.4-full.zip`
    - SHA256 `f86741c7c9a4d5b5a6c09f4f73d25d1bdc9b33d7187452f1553aee86cc6a723a`
  - `external_references\ahmad_live_reference_lab\dist\BidKingHeroRef-v0.1.4-engineering.zip`
    - SHA256 `94ef2d4776738289f4a93d8f0a755691175a36a26a98c03bb81cba4b16a47199`
  - `external_references\ahmad_live_reference_lab\dist\BidKingHeroRef-v0.1.4-public-safe.zip`
    - SHA256 `2095d907ace09b6c84c02aee9012e9f647bc38e132133e28d9410b20d073e4c5`
  - `external_references\ahmad_live_reference_lab\dist\BidKingHeroRef-v0.1.4-SHA256.txt`
  - `external_references\ahmad_live_reference_lab\dist\BidKingHeroRef-v0.1.4-RELEASE_NOTES.zh-CN.txt`
  - 三个 zip 已做 clean unzip smoke：都含包内 UI/monitor exe、`Start-HeroRef.ps1` 默认不依赖 `C:\Python313`，`BidKingHeroMonitor.exe --help` 可运行；public-safe 不含 raw tables，只含 `PUT_TABLES_HERE.txt`。
- “等待对局包”反馈：
  - 朋友状态不是纯 UI-only：`capture_source_status.json` 显示 `active_flows=3`、`raw_packets=9`、`accepted_frames=0`、`rev_not_game_frame=9`；
  - monitor 已看到 BidKing.exe 流量，但没有解析到游戏状态帧；
  - 优先让用户用管理员入口 `-Restart` 启动整条链；若仍然如此，用 `-BroadSniff -IncludeLoopback` 备用启动，进新局/使用道具后直接点 mini 常驻 `导出` 回传。
- 验证：
  - `C:\Python313\python.exe -m pytest tests/test_live_overlay.py -q` -> `141 passed`
  - `C:\Python313\python.exe -m pytest tests/test_live_monitor.py tests/test_ahmad_ref_engine_public_info.py -q` -> `101 passed`
  - `C:\Python313\python.exe -m pytest tests/test_live_overlay.py tests/test_live_monitor.py tests/test_ahmad_ref_engine_public_info.py -q` -> `242 passed`
  - `C:\Python313\python.exe -m pytest tests/test_bid_map_table.py tests/test_live_overlay.py tests/test_live_monitor.py tests/test_ahmad_ref_engine_public_info.py -q` -> `257 passed`
  - `C:\Python313\python.exe -m pytest tests/test_activity_shadow_prior.py tests/test_build_processed_data.py tests/test_basic_mc.py -q` -> `10 passed`
  - `C:\Python313\python.exe -m pytest tests/test_live_overlay.py -q` -> `144 passed`
  - `C:\Python313\python.exe -m pytest tests/test_live_overlay.py -q` -> `146 passed`
  - `C:\Python313\python.exe -m pytest tests/test_activity_shadow_prior.py tests/test_live_overlay.py -q` -> `145 passed`
  - `C:\Python313\python.exe -m pytest tests/test_activity_shadow_prior.py tests/test_build_processed_data.py tests/test_basic_mc.py tests/test_bid_map_table.py tests/test_live_monitor.py tests/test_live_status.py tests/test_ahmad_ref_engine_public_info.py tests/test_live_overlay.py -q` -> `283 passed`
  - `C:\Python313\python.exe -m pytest tests/test_activity_shadow_prior.py tests/test_build_processed_data.py tests/test_basic_mc.py tests/test_bid_map_table.py tests/test_live_monitor.py tests/test_live_status.py tests/test_ahmad_ref_engine_public_info.py tests/test_live_overlay.py -q` -> `280 passed`
  - `C:\Python313\python.exe scripts\build_activity_shadow_prior.py` -> wrote `data\processed\activity_drop_shadow_prior.json`
  - `C:\Python313\python.exe -m py_compile scripts\build_activity_shadow_prior.py external_references\ahmad_live_reference_lab\tools\ahmad_tk_overlay.py tests\test_activity_shadow_prior.py tests\test_live_overlay.py` -> passed
  - `C:\Python313\python.exe -m py_compile src\bidking_lab\extract\bid_map_table.py src\bidking_lab\live\monitor.py external_references\ahmad_live_reference_lab\src\ahmad_ref_engine.py external_references\ahmad_live_reference_lab\tools\ahmad_tk_overlay.py external_references\ahmad_live_reference_lab\tools\ahmad_live_panel_server.py` -> passed
  - `C:\Python313\python.exe -m py_compile external_references\ahmad_live_reference_lab\tools\ahmad_tk_overlay.py` -> passed
  - PowerShell parser 检查 `build_hero_ref_portable.ps1` -> passed
  - Tk introspection 确认 mini `440x397` 下 `导出` 可见，导出 tooltip 包含 `生成诊断 zip` / `发群里` / `log 排查`，`地图` `<Enter>/<Leave>` 预览绑定与点击绑定都在，右侧 hover popup 可显示/隐藏，额外 `map_tip` 不存在。

## 5.3 2026-06-11 发布后 hotfix checkpoint

- 群友反馈的 `Import-LocalTables.ps1` ParserError 已修成包级兼容问题：
  - `build_hero_ref_portable.ps1` 现会把打包后的所有 `*.ps1` 重写成 UTF-8 BOM；
  - 当前 `BidKingHeroRef-v0.1.4-full.zip` 在 Windows PowerShell 5.1 下可正常进入业务校验，不再报 ParserError。
- mini 的“下一步”不再直接露出 `no_raw_packets`，现在显示动作文本；`no_raw_packets` 仍保留在内部诊断里。
- public-safe 边界已再核一次：解压后 `data\raw\tables` 仅有 `PUT_TABLES_HERE.txt` 占位，没有实际 raw tables，也没有回退到系统 Python 的引用。
- 当前 v0.1.4 三包哈希：
  - full: `0cf21a1fb812a5b97241c7d80eeb7aafac07d0bf90e2950a59f140db1a93e44f`
  - engineering: `f6e0a83cf86e884f3d5a56b5f9445bbe35ac455656d892be8b45209955806b78`
  - public-safe: `6f53cc0e4353e0c8d91060241c3a44accd35d24419d9c369a231a51254233039`

## 6. 新窗口 prompt

```text
项目目录：C:\xiangmuyunxing\biancheng\2026\bidking-lab

请先读取：
1. C:\xiangmuyunxing\biancheng\2026\AGENTS.md
2. C:\xiangmuyunxing\biancheng\2026\bidking-lab\handoff_2026-06-11.zh-CN.md
3. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\HANDOFF_2026-06-10.zh-CN.md
4. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\EXECUTION_NOTES_2026-06-10.zh-CN.md
5. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\README.zh-CN.md
6. C:\xiangmuyunxing\biancheng\2026\bidking-lab\PROGRESS_V3.md
7. C:\xiangmuyunxing\biancheng\2026\bidking-lab\DECISIONS_V3.md
8. C:\xiangmuyunxing\biancheng\2026\bidking-lab\OBSERVATIONS_V3.md

先确认：
git status --short
git log -3 --oneline

当前默认先继续 Hero Ref 支线收尾，不推进主线 v3 promotion，不回滚其他窗口或用户修改。主线 v3 只保持 shadow-only / audit-only 记录状态。

Hero Ref 当前目标：
把 BidKingHeroRef 做到稳定可发布和分享的小工具，继续验证 compact Tk UI、manual fallback、live monitor、Ahmad/Victor/Aisha 的 ref_v0 路线。

重点检查：
1. full 包是否能在干净解压后直接运行，不依赖外部 Python；
2. safe / full 包边界是否写清楚；
3. logs 是否落在 data\\logs\\live\\；
4. UI 关闭时 monitor 是否联动关闭；
5. 英雄名、手填、轮次切换、minimap、结算态是否稳定；
6. 1.8 / 1.80 这类显示是否只影响展示，不影响引擎语义；
7. 4406 那类结算差价优先按价格表 / 活动版本 / 外部表漂移检查，不要优先当成 missing settlement block；
8. 朋友反馈已记录在 EXECUTION_NOTES_2026-06-10.zh-CN.md，需要逐条继承，不要重新从聊天里猜。

如果用户明确切回主线 v3，再按 handoff_2026-06-10.zh-CN.md 的主线部分继续；不要把 Hero Ref 的显示口径当成主线 truth，不要恢复 formal/value sampler 正式接入。
```
