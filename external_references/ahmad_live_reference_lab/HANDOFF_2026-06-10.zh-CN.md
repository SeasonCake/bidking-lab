# Ahmad/Victor Hero Ref 支线交接 - 2026-06-10

本文是 Hero Ref 支线的新窗口入口。更细的实战反馈、原样问题记录、我的逐条回应和可复用检查，见 `EXECUTION_NOTES_2026-06-10.zh-CN.md`。

## 0. 先读

1. `C:\xiangmuyunxing\biancheng\2026\AGENTS.md`
2. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\README.zh-CN.md`
3. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\EXECUTION_NOTES_2026-06-10.zh-CN.md`
4. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\handoff_2026-06-10.zh-CN.md`
5. `C:\xiangmuyunxing\biancheng\2026\bidking-lab\docs\PROJECT_STRUCTURE_V3.zh-CN.md`

先确认：

```powershell
cd C:\xiangmuyunxing\biancheng\2026\bidking-lab
git status --short
git log -3 --oneline
```

## 1. 当前判断

- Hero Ref 已经不是“纯实验”，而是接近发布和分享的可实战小工具。
- 主线 v3 仍然保持 `shadow-only` / `audit-only`，不要在这条支线上把正式出价或主线 promotion 一起改掉。
- 当前 Hero Ref 的重点是收尾，不是再开一轮大改。

## 2. 当前已经确认的映射口径

- `100204` -> Ahmad 总件数。
- `1002041/1002042/1002043` -> Ahmad 的金 / 紫 / 蓝均格输入。
- `1002044` -> Ahmad 的白绿件数。
- `100209` -> Victor 的 `q4 + q5 + q6` 件数之和，不再按旧的 `q4 + q5` 理解。
- `0x002D` settlement payload 不是隐藏的 capacity source，里面更像是 metadata + 完整 inventory。
- `fixed_counts` 只约束件数；`avg_cells` / `quality_cells` 约束格数；`value` 继续单独看。
- `0` 均格是合法输入，不要当成缺失。
- `1.8` 和 `1.80` 在 UI 展示上可以 compact，但引擎语义上要保持 exact float，不要把它们当成两个不同或两个相同的 truth，核心是“用户输入保留精度，引擎做 exact/compact 分层”。

## 3. 当前可直接复用的检查结果

- `load_monitor_tables()` / `BidMap` parser 已确认 4406 family 是可识别的，不是缺 map row。
- 4406 的 `0x002D` settlement inventory 是完整的 24 件，`ui_contract.minimap.layout_complete=true`。
- 这局里游戏侧显示的结算价格和本地值不一致，更像价格表 / 活动版本 / 外部参考表漂移，不像 settlement block 丢了。
- `capture_source_status.json` 没有 `dropped_bytes`，monitor 本身没有明确丢包证据。
- `sparse_exact_prior` 快路径已经能用于 total_count 已知但分裂稀疏的局，不要再靠 `max_combos` 截断冒充优化。
- 结算旧局、切换新局、手填覆盖、实时更新都要继续保持联动，不要把 manual 变成孤立快照。

## 4. 这条支线的当前目标

把 Hero Ref 做到可稳定发布的版本，不再扩成复杂的新引擎：

- compact Tk UI 保持小窗口可读；
- manual fallback 继续可用，但不打断 live；
- Ahmad / Victor / Aisha 三个英雄都能稳定识别、填写、清空、切轮次；
- full 包可直接给别人解压运行；
- `safe` 包仅在不带本地原表时使用，并明确提示用户自己导入表文件；
- release 说明要写清楚 logs 路径、管理员权限、WinDivert / 火绒信任边界。

## 5. 下一窗口建议优先做的事

1. 先做低风险 UI 信息重排：
   - `tools/ahmad_live_panel_server.py::summarize_snapshot` / `_ref_input_summary`
   - `tools/ahmad_tk_overlay.py::render`
   - 目标是把 mini 里的“来源 / 状态”降级，把“当前不确定什么”前置；不要改主线 v3 sampler。
2. 再做低品信息提示：
   - 基于已有 `quality_count_ranges` / `quality_cells_ranges` 给白绿 / 蓝 / 紫 / 金 / 红不确定性提示；
   - 不新增新引擎，不把低品提示变成正式出价输入。
3. 公共抽检 / outline 亮红：
   - 有 item_id / item_name 才显示具体物品；
   - 只有 quality / outline 时只显示品质下界和格数，不编造物品名。
4. 手填区只做视觉分块：
   - 入口 `tools/ahmad_tk_overlay.py::_build_manual_panel`；
   - 不改变 manual key / bridge contract。
5. 重新跑一轮最小 smoke，确认 Ahmad / Victor / Aisha 的英雄名、手填、清空、轮次切换没有回退。
6. 本轮补丁后重新打包，再验证 full 包可直接运行，解压后日志是否固定在 `data\logs\live\`。
7. 检查 UI 关闭时 monitor 是否联动关闭，除非显式 `-KeepMonitorOnClose`。

更细的路径和验证清单见 `EXECUTION_NOTES_2026-06-10.zh-CN.md` 第 7 节。

## 6. 新窗口 prompt

```text
请继续 Ahmad/Victor Hero Ref 支线，但不要改主线 v3 promotion，也不要回滚其他窗口已经做好的主线改动。

项目目录：C:\xiangmuyunxing\biancheng\2026\bidking-lab

先读取：
1. C:\xiangmuyunxing\biancheng\2026\AGENTS.md
2. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\README.zh-CN.md
3. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\HANDOFF_2026-06-10.zh-CN.md
4. C:\xiangmuyunxing\biancheng\2026\bidking-lab\external_references\ahmad_live_reference_lab\EXECUTION_NOTES_2026-06-10.zh-CN.md
5. C:\xiangmuyunxing\biancheng\2026\bidking-lab\handoff_2026-06-10.zh-CN.md

先确认：
git status --short
git log -3 --oneline

当前目标：
把 Hero Ref 做到稳定可发布的小工具，优先验证 compact Tk UI、manual fallback、live monitor、Ahmad/Victor/Aisha 的 ref_v0 路线。

重点检查：
1. full 包是否能在干净解压后直接运行；
2. safe / full 包边界是否写清楚；
3. logs 是否落在 data\\logs\\live\\；
4. UI 关闭时 monitor 是否联动关闭；
5. 英雄名、手填、轮次切换、minimap、结算态是否稳定；
6. 1.8 / 1.80 这类显示是否只影响展示，不影响引擎语义。
```

## 7. 2026-06-11 补充 checkpoint

- 当前用户已确认 Hero Ref 小版本整体达到可发布要求；上一轮已打 `engineering / portable / public-safe` 三类包，但包来自 dirty worktree checkpoint，后续重新打包时必须重新报告 commit、dirty 状态、路径和 SHA256。
- 新反馈里，结算偶发不完全仍保留为待样本回查项；没有新的 `data` 路径或导出包前，不要先改 parser，也不要优先当成 missing settlement block。
- 下一批低风险 UI 项：
  1. 把红件、红格、紫 / 金件数、白绿蓝等候选值从默认小到大显示改成用户更容易读的从大到小显示；只改显示顺序，不改引擎集合和估值语义。
  2. 给 Tk overlay 增加可选 taskbar 模式，让用户可通过任务栏 / `Alt+Tab` / `Win+Tab` 切换；默认仍保持当前悬浮 overlay。
- 继续遵守：主线 v3 只保持 shadow-only / audit-only，不把 Hero Ref 展示口径当成主线 truth。
