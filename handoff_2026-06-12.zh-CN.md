# Handoff 2026-06-12 — Maria wiring + 金均格 0 调查 checkpoint

## 1. 本 checkpoint 目标

- 记录 Maria skill pipeline 与 Ahmed 金均格 0 / UI 调查结论。
- 把现行结果与执行记录写入对应文档（`EXECUTION_NOTES` §52–§54、`hero_ref_settlement_sample_index` §8）。
- Git commit + push `main`，作为后续分批修风险点的基线。

## 2. 已落地代码（本 commit）

### Maria（hero 108）

- `fatbeans.py`：`_maria_skill_updates()`、`10010801` 粗品 grid marker、`100108` 白 tier value（`10010802/03` 预留）。
- `monitor.py`：artifact 含 `skill_reveal_rows` / `skill_reveals`。
- `snapshot.py`：minimap 从 skill reveals 取 marker。
- `ahmad_ref_engine.py`：`_apply_maria_skill_evidence()` + `maria_skill_*` notes。
- Tests：Maria 相关 + 既有 ref/live 回归。

### 金均格 0 调查（**未改行为**）

- 机制文档化：SEND-only 无 REV → 常无 inferred_zero → prior 残留。
- Recordings 125 reset 审计：无 `100113`、无显式 gold-zero evidence。
- 手动路径 / q4q5_count / red display pairing 列为待批风险点。

## 3. 文档索引

| 文档 | 内容 |
|---|---|
| `external_references/ahmad_live_reference_lab/EXECUTION_NOTES_2026-06-10.zh-CN.md` | §52 Maria、§53 金均格 0、§54 分批计划 |
| `docs/hero_ref_settlement_sample_index_2026-06-11.zh-CN.md` | §8 recordings 审计 + SEND-no-REV |
| `handoff_2026-06-11.zh-CN.md` | 06-11 主线；本文件为 06-12 增量 |

## 4. 验证命令（checkpoint 前）

```powershell
cd c:\xiangmuyunxing\biancheng\2026\bidking-lab
python -m pytest tests/test_ahmad_ref_engine_public_info.py tests/test_live_fatbeans.py tests/test_live_monitor.py -q
```

## 5. 下一批工作（用户指定：分 batch）

见 `EXECUTION_NOTES` §54：A inferred_zero 时序 → B 手动 UX → C public avg fallback → D red display pairing。每批需样本 + 期望 UI 再改。

## 6. 已知未确认

- Maria `10010802` / `10010803` 与绿/蓝 tier 映射：待用户 R1 后 skill 导出。
- 金均格 0 UI bug：缺「SEND-only + 后续 state」同 session 的 gold avg 样本；recordings 未覆盖。
