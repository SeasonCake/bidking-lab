# Handoff 2026-06-12 — Maria wiring + 金均格 0 调查 checkpoint

## 1. 本 checkpoint 目标

- 记录 Maria skill pipeline 与 Ahmed 金均格 0 / UI 调查结论。
- 把现行结果与执行记录写入对应文档（`EXECUTION_NOTES` §52–§56、`hero_ref_settlement_sample_index` §8）。
- Git commit + push `main`，作为后续分批修风险点的基线。

## 2. Git 基线

| 项 | 值 |
|---|---|
| Branch | `main` @ `origin/main` |
| Checkpoint commit | `32f4bb3` — *Checkpoint Maria skill wiring and gold-zero investigation notes.* |
| 含此前未 push | `3cebdc2` hotfix2 引擎/UI → `dcb1bd1` monitor 生命周期 → `f9fca54` hotfix2.1 记录 |

## 3. 已落地代码（`32f4bb3`）

### Maria（hero 108）

- `fatbeans.py`：`_maria_skill_updates()`、`10010801` 粗品 reveal（无轮廓 coarse location）、`100108` 白 tier value（`10010802/03` 预留）。
- `monitor.py`：artifact 含 `skill_reveal_rows` / `skill_reveals`。
- `snapshot.py`：minimap 从 skill reveals 取 marker。
- `ahmad_ref_engine.py`：`_apply_maria_skill_evidence()` + `maria_skill_*` notes。
- **语义**：`public_info_200027` = 公开摇号，≠ Maria skill。

### 同 commit 内、笔记曾缺条（见 EXECUTION_NOTES §55）

- **16 英雄**识别 + 非三英雄 `generic_ref_hero` 通用 nest-tier。
- **200048 公开最高品质 ≤5** → `q6=0`（与 v2 monitor 对齐）。

### 更早 commit 已落地（§45–§49）

- 红件 `3/3/3` 不折叠（`_red_range_text`）。
- 公开精确 `200009–200020`、显示均格 `2.09` 兜底。
- WinDivert open 失败诊断、UI 关则停 monitor、hotfix2.1 sparse prior。

### 金均格 0 调查（**未改行为**）

- 机制文档化：SEND-only 无 REV → 常无 inferred_zero → prior 残留。
- Recordings 125 reset 审计：无 `100113`、无显式 gold-zero evidence。
- 手动路径 / q4q5_count / red display pairing 列为待批风险点。

## 4. 文档索引

| 文档 | 内容 |
|---|---|
| `docs/HERO_REF_FILE_AND_DOC_INDEX.zh-CN.md` | **代码+文档总索引**（数据流、各文件何时更新） |
| `EXECUTION_NOTES_2026-06-10.zh-CN.md` | §43–§49 发版链；§52–§58；§55 落地表；**§57 小地图** |
| `docs/hero_ref_settlement_sample_index_2026-06-11.zh-CN.md` | §8 recordings；§9 金为零；**§11 小地图 live** |
| `handoff_2026-06-11.zh-CN.md` | 06-11 主线 |

## 5. 验证命令

```powershell
cd c:\xiangmuyunxing\biancheng\2026\bidking-lab
python -m pytest tests/test_ahmad_ref_engine_public_info.py tests/test_live_fatbeans.py tests/test_live_monitor.py -q
```

Checkpoint 前：**216 passed, 25 skipped**。

## 6. 下一批工作（分 batch）

| 批 | 内容 | 文档 |
|---|---|---|
| A | inferred_zero 时序（SEND-only 无 REV） | §54 |
| B | 手动 UX / q4q5_count reconcile | §54 |
| C | public avg `200013–200016` fallback | §54 |
| D | q6 锁 `[0,0,0]` 时 red display pairing | §54 |
| **E** | **底层抓包：WinDivert → 新架构**（火绒/360 误杀） | **§56** |

每批需样本 + 期望 UI（A–D）；E 批需 capture 接口设计 + 双轨 jsonl 对比。

## 7. 未落地 / 待样本

- Maria `10010802` / `10010803` 绿/蓝 tier。
- Raven 无轮廓全品质 skill pipeline（**低优先级，暂缓**，见 EXECUTION_NOTES §59）。
- 伊森：166 份 Fatbeans 样本已审计；支线已接 skill 标签 / R2–R4 gating / ref R5 evidence（工作树，§59）。
- 金均格 0 UI：缺同 session gold avg SEND-no-REV 样本（**用户 2026-06-12 进行中**，收到后写 sample index §9）。
- 桌面包 exe：源码已 push，需重打包 hotfix2.1+ 才含 Maria / 16 英雄等。

## 8. 聊天记录核实摘要

完整矩阵见 **EXECUTION_NOTES §55**。本次补录：**16 英雄 + 200048→q6=0**（原仅有代码与测试，笔记无独立 §）。新增规划：**§56 非 WinDivert 抓包迁移**（用户明确要求列入计划）。

## 9. 2026-06-12 晚 checkpoint — 小地图 / 至宝估价（**工作树未 commit**）

### 9.1 用户验收

- **加布里** session `2402:1425860548836801`（2402 设计师居所）：R3 **至宝估价** local 25 → **71,500**；结算 Q6 **大溪地黑珍珠** 同格；ref `q6=3/3/3`；**用户确认小地图显示 OK**。
- 同批修复已在 **索菲 / 塔蒂安娜 / 吴起灵** live 局验证（见 EXECUTION_NOTES **§57**、sample index **§11**）。

### 9.2 已改文件（相对 `32f4bb3`）

| 区域 | 文件 |
| --- | --- |
| Parse | `src/bidking_lab/live/fatbeans.py` |
| Artifact / contract | `src/bidking_lab/live/monitor.py`, `src/bidking_lab/runtime/snapshot.py` |
| Ref | `external_references/.../ahmad_ref_engine.py` |
| UI | `external_references/.../tools/ahmad_tk_overlay.py`, `ahmad_live_panel_server.py` |
| Tests | `tests/test_runtime_snapshot.py`, `test_live_fatbeans.py`, `test_live_overlay.py`, `test_ahmad_ref_engine_public_info.py` |

### 9.3 验证

```powershell
cd c:\xiangmuyunxing\biancheng\2026\bidking-lab
python -m pytest tests/test_runtime_snapshot.py tests/test_live_fatbeans.py tests/test_live_overlay.py -q -k "minimap or treasure"
```

Focused 子集绿；全量 overlay+ref 跑时 378 passed（另有 1 条 public_info 回归与本次无关，待查）。

### 9.4 下一批

| 批 | 内容 |
| --- | --- |
| F | commit + push 小地图批次；重打包 Hero Ref exe |
| G | `organize_hero_ref_samples.py --apply` 归档加布里 export → manifest §11 |
| H | Hero pool ref bridge（加布里 / 塔蒂安娜 item-anchor；吴起灵 `100207`） |
| A–E | 仍见 §6（金均格 0、抓包迁移等） |

## 10. 2026-06-12 晚 — §50 性能结论（**暂缓继续优化**）

| 项 | 结论 |
|---|---|
| Git | `main` @ **`a0135c7`**（§50-2 + Aisha next-info + audit 路由），已 push |
| Ahmed R2+（总件+均格） | warm **~37–97ms**，**可接受**，不必为发版继续抠 |
| R1 `sparse_exact_prior` | avg ~600ms，p95 ~1.5s — **记录待办**，非阻塞 |
| Ethan generic ref | **仍搁置** |
| 精度门禁 | 不改 `max_combos`；perf 改动绑 fatbeans `balanced`+`combo_count` |

详见 **EXECUTION_NOTES §60**。

## 11. 下一主线 — **艾莎体验 + 准确度**（§61）

**目标**：按群友反馈逐步扩大艾莎计算器适用范围；每批样本 + settlement 门禁。

| 批 | 内容 | 优先级 |
|---|---|---|
| **A** | 艾莎 **筛选后** fatbeans gap audit（`audit_aisha_gap.py`）；sample index §12 仅 15–25 条代表局 | ✅ 初跑完成 |
| **B** | v0 准确度：**总格收束**、红值下界、0052 fallback、白绿 live bridge | P1 |
| **C** | 体验：R1 hero 识别、mini 摘要与 next-info 一致 | P1 |
| **D** | 艾莎 count_prior 性能（2407 极端局） | P2（按需） |
| **E** | 新地图/轮次扩展 | P2–P3 |

**批 A 结论（173/244 样本，见 EXECUTION_NOTES §61）**：最大缺口是 **总格（~69% miss，总件精确时仍 ~67%）**；金/红件数在总件锁定后明显改善；**红值**仍 ~20–40% miss。不全库 audit；排除 1 轮、高 tail、证据不足局。

**v0 优先**：总格 residual + 红值；**v3 延后**：品质桶、形状限制。

**已就绪（`a0135c7`）**：`_aisha_next_info_hint` 工具顺序提示；白绿 split 引擎 + 大量单元测试。

**暂缓**：§50 sparse prior 收紧、§50-3、§46 字体缩放、Ethan perf。

详见 **EXECUTION_NOTES §61**。
