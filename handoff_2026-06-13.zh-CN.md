# Handoff checkpoint — 2026-06-13（艾莎 Phase 1 live + **v0.1.8 已发布**）

> **v0.1.8 之后变更**：[`CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md`](CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md)（逐条记录，默认不打包，下一对外版 v0.2.0）
> **主策略**：[`docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md`](docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md)
> **代表样本/门禁简表**：[`docs/hero_ref_aisha_representative_samples_2026-06-13.zh-CN.md`](docs/hero_ref_aisha_representative_samples_2026-06-13.zh-CN.md)
> 本文件 = 当前唯一权威 checkpoint；旧 handoff（06-08~06-12）仅历史参考。

---

## 0. 一句话现状（2026-06-13 晚更新）

**Hero Ref v0.1.8 已打包并 push**（full + public-safe，源 commit `a706bd5`，干净树）。本轮在 Phase 1 基础上又修了一个实战估价 bug（白绿件数随机揭示被误当确定值）+ 一批 UI/打包问题，并把空闲快照升级为完整推理。公告 / 非公告说明 / GitHub release note 已交付（夸克链接用户自填）。
艾莎 **Phase 1 仍 live**（B1/B2 总格 + C1 报价门禁 + C2 band + D1 shadow + 产品防守倍数 UI）。格/件区间相对无优化有提升、零门禁退化；balanced ~83% miss 仍是结构性问题，留 v0.2。
**全量 pytest 全绿**，新增白绿 floor 回归测试。**已知遗留**：无「总件揭示」的英雄（如艾莎）在偏大局面早轮估价偏保守（count_prior 中心标定，V3 架构层面）→ 列 v0.2.0。

---

## 1. Git 状态

| 项 | 值 |
|---|---|
| 分支 | `main`，**已与 `origin/main` 同步** |
| HEAD | `2d51012`（见 [`CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md`](CHANGELOG_HERO_REF_post-v0.1.8.zh-CN.md)） |
| 远程 | `2d51012` @ `origin/main` |
| 上次**对外**打包 | **`v0.1.8` @ `a706bd5`（2026-06-13）** |
| v0.1.8 之后 | **逐条记入 CHANGELOG，默认不打包**；下一对外版计划 **v0.2.0** |

v0.1.8 包 SHA256：full `1715910C…E4278`（42.1MB）/ public-safe `B5C7DA03…2275`（38.5MB）。
样本库：已并入 140 个新有效实战样本（497→637，本地 data，不进 git）；12 个丢包/多局混合样本排除。
未提交（均为 audit 输出，**不必进 git**）：`data/reports/audit_aisha_*.txt` 等（脚本可重生成）。

---

## 1.5 v0.1.8 本轮新增（2026-06-13 晚，Phase 1 之后）

| 项 | 内容 | 文件 | 证据 |
|---|---|---|---|
| **白绿 floor 修复** | 公开/技能随机品质揭示（未知别墅 200026-8、宝光四鉴 100136）不再锁定 q1；无精确来源时只作下限（floor） | `_apply_low_quality_split_evidence`（`ahmad_ref_engine.py`） | 新增 `test_ref_engine_coarse_public_split_reveal_does_not_lock_q1`、`..._without_exact_q1_stays_a_floor`；艾莎抽测 0 回归 |
| 空闲 full 升级 | 局面稳定且空闲时把 fast(10) 快照自动升级 full(500) 推理（带耗时预算） | `run_fatbeans_webhook_monitor.py` `_maybe_run_idle_full_upgrade` | `test_idle_full_upgrade_*`（5 条） |
| 每轮滚动快照 | 诊断导出含 `round_snapshots/`，便于复现定位 | monitor + overlay `_candidate_diagnostic_paths` | `test_round_snapshot_archive_*`、`..._export_includes_round_snapshots` |
| UI 重开覆盖 | 初始布局按空 widget 测量→重开底部被遮，改为渲染后 `_settle_initial_layout` 重新贴合 | `ahmad_tk_overlay.py` | `test_settle_initial_layout_*`（3 条） |
| 小地图锚点/闪烁 | 裸品质揭示（拉文）锚点偏移修复 + 签名缓存防每秒重绘 | `fatbeans.py`、overlay | `test_bare_quality_reveal_uses_shaped_anchor_not_scan_cell` 等 |
| 静默启动/关闭崩溃/驱动卸载 | 恢复 `-WindowStyle Hidden`；关闭 guard `winfo_exists`；退出卸载随包 WinDivert（解压目录可删） | `Start-HeroRef.ps1`、overlay、`Stop-HeroRef.ps1` | 关闭/卸载回归测试 |
| 艾莎下一步文案 | 按已知品质动态提示（等待X品/总格总件），不再机械推道具 | `ahmad_live_panel_server.py` | 文案断言更新 |
| 多英雄识别 | `HERO_BY_ID` 全 20 英雄走估价（通用 ref）；Ahmed/Aisha/Victor 结构化深度 | `ahmad_ref_engine.py` | — |

**白绿 bug 影响**：实测样本 R1 balanced 782,399→578,318（修正），后续轮不再因约束冲突「无报价」。

---

## 2. 已完成（live 生效）

| 批 | 内容 | 引擎/文件 | 证据 |
|---|---|---|---|
| B1 | 高档 quality_cells → `total_grid_target` | `_apply_total_grid_target_from_known_high_tier_cells` | 7-band 不回归 |
| B2 | 残差 unfixed tier `avg_cells` 估格，无信号→4.0 | `RESIDUAL_AVG_CELLS_NOTE` | synthetic + 7 不回归 |
| B3/B4 | 0052 r3 白绿 bridge + public avg downgrade | `test_..._0052_..._regression` | pytest |
| B5 | UI 低品件/格 + 署名 | overlay | overlay pytest |
| C1 | good_regression：锁 `combo_count`+`balanced`，gap 不劣化 >15% | 2 条 fatbeans | pytest |
| **C2 band** | R3+ 只扩总格 g90 上界（**Aisha only**） | `audit_aisha_layout_mode=band` | 148:34.5%→43.9% (+14/0) |
| C2 shadow | footroom notes → UI「布局余量」 | panel + overlay | — |
| **D1 shadow** | round×band 宽度 → 建议 q6 折扣 **note**（**不改 balanced**） | `audit_aisha_d1_mode=shadow` | 148/148 行有 note |
| 产品 | R1–R5 防守×倍数 UI（2.0/1.6/1.3/1.1/1.1） | `_aisha_defense_multiplier_hint` | overlay pytest |

**live 默认装配**：`prepare_reference_engine_snapshot()` 对 hero=aisha 注入 `layout=band` + `d1=shadow`（显式值可覆盖；Ahmed/Victor 不注入）。调用方：`ahmad_live_panel_server.py`、`ahmad_tk_overlay.py`（含 manual 路径）。

---

## 3. 已验证（艾莎相关子集门禁全绿，2026-06-13）

```powershell
cd c:\xiangmuyunxing\biancheng\2026\bidking-lab
python -m pytest tests/test_ahmad_ref_engine_public_info.py -k "aisha_batch_b or 0052 or layout or good_regression or prepare_reference or d1_" -q   # 21 passed
python -m pytest tests/test_live_overlay.py -k "aisha or defense_multiplier or layout or quality_uncertainty or d1_flag" -q                          # 通过
python scripts/audit_hero_layout_isolation.py            # OK，Ahmed 12 样本无 delta
python scripts/audit_aisha_band_gate_smoke.py            # worsened 0 / improved 0；0052 不变
python scripts/audit_aisha_round_cohorts_sample20.py     # +4/0（见下），234s
# hidden 2601 perf：2 passed <2s
```

**sample20**（99 rows）：R1/R2 15%→15%（layout 不生效）；R3 20%→30% (+2)；R4 15%→20% (+1)；R5 15.8%→21.1% (+1)；合计 **16.2%→20.2% (+4/0)**。balanced miss 95–100%（Phase 2）。

### ✅ 全量 pytest 已全绿（10 个失败全部修复，发版阻塞解除）

`pytest tests/` → **1542 passed, 25 skipped, 0 failed**（含 `test_ahmad_ref_engine_public_info.py` + `test_live_overlay.py` 332 passed）。

排查方法：用 `git worktree` 在引入/破坏 commit 上复跑（非破坏性），区分真回归 vs 添加即破损。

| 用例 | 现象 | 根因（worktree 验证） | 修法 |
|---|---|---|---|
| `..._public_red_reveal_value_and_cells_are_floors` | no_reachable_combo≠ok | **真回归**：`a0135c7`绿→B1`9982365`破。B1 高阶格 target 忽略公开揭示 q6 cell 下限(15)，target=142<最小152 | 引擎：B1 `_apply_total_grid_target_from_known_high_tier_cells` **gate 到 aisha** |
| `..._partial_known_red_value_includes_unknown_estimate` | count_prior≠ok | **真回归**：B1 + `ffbb038`残差估计；叠加 `d40a3f1` pinned sparse-prior 路径把 ahmed pinned 态转 count_prior | 引擎：B1 gate aisha + `_should_use_sparse_exact_total_prior` 的 pinned 分支 gate aisha |
| `..._partial_known_red_data6_style_above_known_not_flat` | red_value_range None | 同上（B1） | 同上 |
| `..._public_q5_avg_value_and_count_zero_from_public_numeric_facts`(hero=maria) | note `zero_avg_value_q5_count_zero` 缺 | **测试欠债**：在引入它的 `bb588ab` 即失败、从未通过。q5=0 已由 `public_info_200019_q5_count` 正确锁定，该 note 本不可达 | 测试：删除不可达断言（引擎行为正确） |
| tk `..._minimap_unknown_footprint_*` / `*_draws_explicit_marker_*` | DummyCanvas 缺 `update_idletasks` | 测试 fixture 滞后（`canvas_draw_size` 引入后未更新） | 测试：DummyCanvas 加 `update_idletasks` 空操作 |
| tk `..._render_missing_uses_action_text_*` / `*_surfaces_windivert_open_error` | Widget 缺 `bind`（HoverTip 截断时挂） | 同上 fixture 滞后 | 测试：Widget 桩加 `bind` 空操作 |
| `test_minimap_table_shape_requires_cell_count_match`（全量额外发现） | row=2≠None | **真回归**：`bb588ab` 加的 local_index 回退无视 shape 一律放置 | `monitor.py`：local_index 回退加 `shape_key 已解析` 条件（已知形状/value-only 仍放置，未知形状不放置） |
| `test_hero_ref_non_ascii_batch_files_are_cmd_safe`（全量额外发现） | .bat 非 CRLF | **本地工作树 LF 漂移**（非代码）：repo 存 LF + `autocrlf=true`，干净 checkout/打包(`Write-Utf8NoBomCrLfFile`)均为 CRLF | `git checkout -- *.bat` 让 autocrlf smudge 落地 CRLF，零仓库改动 |

**确认**：B1 与 pinned sparse-prior 都是 **aisha 专项优化**误泄漏到 ahmed（违反 ahmed 隔离红线）；gate 后 aisha 用例（band/good_regression/`pinned_quality_cells_sparse_prior` hidden-2601、2700 行 target 断言）全部保持通过。generalize B1 至「floor-aware 全英雄」列入 v0.2。

---

## 4. 批测结论（关键决策依据）

| 方向 | 结果 | 决策 |
|---|---|---|
| C2 **band** | 148:+9.4pp，0 退化 | ✅ live |
| C2 **target** 抬点估计 | 各 cohort 更差 | ❌ 不 live |
| **D1 apply** 压 q6（模拟） | balanced hit 18.9%→**8.8%（−10.1pp，恶化）**；improved 5 / worsened 20 | ❌ 不 live（公式方向反） |
| D1 **shadow** | balanced 不变，仅 note | ✅ live（诊断） |

**为何 apply 反**：主导误差是 **under 56%**（结算价 > ref balanced）；再压低 q6 尾部方向相反。
工具：`scripts/audit_aisha_bid_by_round.py` → `data/reports/audit_aisha_bid_by_round.txt`

---

## 5. 推理效率（`audit_aisha_perf.py`，25 样本×R3–R5）

| 轮次 | off avg | live avg | ≥2s | R4 总仓储 |
|---|---|---|---|---|
| R3 | 289ms | 146ms | 0 | 2/21 |
| R4 | 135ms | 129ms | 0 | 128ms@combos=253 |
| R5 | 84ms | 50ms | 0 | — |

- band + D1 shadow 对引擎 **avg −64ms**（非卡顿源）
- 群友「R4 总仓储卡」**本批未复现**：2504 混局 ≈128ms；2601 hidden 走 `exact_total_avg_cells_fast_path` ≈7ms
- 仍可能慢：**宽 `count_prior` combo**（本批 max 1079ms，未超 2s）；Ahmed `sparse_exact_prior` 全库仍见 1–2.5s
- 若 live 仍卡：看导出包 `summary_total_ms`/`ref_engine_ms` 区分引擎 vs Tk 主线程（UI worker 未做，§7.17）

---

## 6. 未完成 / 待办（按优先级）

### 发版 ✅ v0.1.8 已完成（2026-06-13）
- [x] 全量 pytest 全绿
- [x] `build_hero_ref_portable.ps1 -Version 0.1.8 -Zip`（full + public-safe，源 `a706bd5` 干净树）
- [x] push `origin/main` @ `a706bd5`
- [x] 公告 / 非公告说明 / GitHub release note 已交付（夸克链接用户自填）
- 注：版本号无代码常量，仅打包标签（0.1.7→0.1.8）；旧 v0.1.7 zip 仍在 dist/，可删。

### v0.2 艾莎/通用重点（发版后）
| 优先级 | 内容 | 替代/状态 |
|---|---|---|
| **P0/P1** | **count_prior 中心标定**：无「总件揭示」英雄（艾莎）偏大局早轮系统性低估（实测 R2 给 6w）。map 家族默认 total（24xx=28）+ 窄枚举窗，被已知低品占满后给高价品留不下空间 | 🔬 已定位（V3 架构级，风险高，全英雄/全图影响），v0.2 深入 |
| P1 | D1 **按 over/under 分 cohort 重做**（apply 当前公式恶化） | shadow 已 live；apply ❌ |
| P1 | cells 继续收束（C2 family 内） | band 已 live |
| P1 | B1 `_apply_total_grid_target...` 改 floor-aware 后再放开到全英雄（当前仅 aisha） | 📋 |
| P2 | C3a 位置 hint（宝光/抽检/footroom），同 C2 批测框架 | 📋 未开 |
| P2 | **count_prior 宽 combo 推理效率** | 性能主风险 |
| P2 | 扩 fatbeans/live 样本 n（已 637） | 并行人工 |
| P3 | UI summary worker（Tk 卡顿，§7.17） | 📋 |

### 本轮发现的小优化
- **D1 shadow UI 噪音** → ✅ **已修**：新增 `_aisha_d1_flag_detail`（阈值 `AISHA_D1_FLAG_DISCOUNT_THRESHOLD=0.7`），shadow note 仅在 discount<0.7 时上 flag；apply note 总显示。引擎仍保留完整 note 供 audit。test：`test_ahmad_server_aisha_d1_flag_detail_filters_weak_shadow_discounts`。
- **D1 apply 三档不一致**（已知限制，仅 audit，**未改**）：apply 只调 `p50`，未同步 `p25/p75`，极端折扣下 balanced 可能跌破 conservative。apply 已被批测否决，重做 D1 时一并处理 conservative/aggressive。

---

## 7. 因耗时未跑完、但方案保留

| 脚本 | 状态 | 替代 |
|---|---|---|
| `audit_aisha_round_cohorts.py` 全库 | ❌ >60min 多次中断 | `sample20` / `c2_batch --audit-round N` |
| `audit_data7_perf --hero aisha --max-round 5` 全库 | ❌ >5min 中断 | `audit_aisha_perf.py --sample-limit 25` |

---

## 8. 红线（全程不变）

- 不在 14 exact-total / 173 curated 上扫权重；引擎改动 = 语义规则 + synthetic + 已 hit 不回归
- 不动 Ahmed/Victor 等其它英雄（layout/d1 仅 Aisha）
- 不 live：C2 target、D1 apply（当前公式）
- v0 不追求 80–90% 格准 / 14-14 band；追求区间可用、报价有参考、门禁不退化

---

## 9. 关键文件索引

| 类别 | 路径 |
|---|---|
| 引擎 | `external_references/ahmad_live_reference_lab/src/ahmad_ref_engine.py` |
| 面板/overlay | `tools/ahmad_live_panel_server.py`、`tools/ahmad_tk_overlay.py` |
| 引擎测试 | `tests/test_ahmad_ref_engine_public_info.py`（layout/band/d1/0052/good_regression/hidden-perf） |
| overlay 测试 | `tests/test_live_overlay.py`（防守倍数/布局余量/minimap） |
| audit | `scripts/audit_aisha_{gap,c2_batch,bid_by_round,perf,round_cohorts_sample20}.py`、`audit_hero_layout_isolation.py`、`audit_aisha_band_gate_smoke.py` |
| 打包 | `external_references/ahmad_live_reference_lab/build_hero_ref_portable.ps1` |
| 执行细节 | `external_references/.../EXECUTION_NOTES_2026-06-10.zh-CN.md`（§61–63 艾莎，§7.17 UI 卡顿） |
