# Handoff checkpoint — 2026-06-13 (C2 band live + 20-sample cohort)

> **主策略**：[`docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md`](docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md)

## 本 checkpoint 做了什么

1. **Phase 1 live**：Aisha overlay/panel 经 `prepare_reference_engine_snapshot()` 默认 `layout=band`（Ahmed/Victor 不变；显式 `audit_aisha_layout_mode` 仍可覆盖）。
2. **UI**：`aisha_layout*` notes → flag「布局余量」（shadow 诊断，与 band 同批 note）。
3. **抽测脚本**：`scripts/audit_aisha_round_cohorts_sample20.py`（~20 全轮次+代表样本，四批 R1/R2/R3/R4–R5，off vs band）。
4. **报告**：`data/reports/audit_aisha_round_cohorts_sample20.txt`（跑脚本后更新）。

## 20 样本抽测（`audit_aisha_round_cohorts_sample20.txt`，231s）

| 批次 | n | cells off → band | band Δ |
|------|---|------------------|--------|
| R1 | 20 | 15% → 15% | +0/0 |
| R2 | 20 | 15% → 15% | +0/0 |
| R3 | 20 | 20% → 30% | +2/0 |
| R4 | 20 | 15% → 20% | +1/0 |
| R5 | 19 | 15.8% → 21.1% | +1/0 |
| **合计** | 99 rows | **16.2% → 20.2%** | **+4/0** |

对比 148 curated penultimate：34.5% → 43.9%（+14/0）。抽测绝对 hit 更低（样本偏 4–5 轮局），**相对 band 增益一致且无退化**。

## 已确认（相对「完全不做 C2」baseline = layout off）

| 范围 | cells hit off → band | 退化 |
|------|----------------------|------|
| 148 curated penultimate | 34.5% → 43.9% | 0 |
| 7-band gate + 0052 + good_regression | 不变 | 0 |
| Ahmed 12 样本 | 无输出差 | — |

**target 抬格**：批测否决，不 live。

## 四批评审（抽测 + 代理，非全库）

| 批次 | 用途 | 抽测/代理 |
|------|------|-----------|
| R1–R2 | 防守价（2.0×/1.6×），**不卡精度** | 抽测脚本；layout 不生效 |
| R3 | 预拿仓，band 可帮 | 代理 n≈20：10%→30% cells |
| **R4–R5** | **Phase 1 主 gate** | R4/R5 分 slice；148 汇总见上 |
| Phase 2 | D1 红品权重 / balanced | **刻意未做** |

## 门禁（merge 前）

```powershell
python -m pytest tests/test_ahmad_ref_engine_public_info.py -k "aisha_batch_b or 0052 or layout or good_regression" -q
python scripts/audit_hero_layout_isolation.py
python scripts/audit_aisha_round_cohorts_sample20.py
```

## 下一刀（Phase 2，cells 稳后再开）

- D1 shadow：`round` + cells band 宽度 → 动态压低 q6 对 balanced 贡献
- `audit_aisha_bid_by_round.py`（仅 balanced ±15%，不动 cells gate）
- 产品防守倍数 2.0/1.6/1.3/1.1×：**UI 提示**，不进引擎扫参

## Git

- 本地 commit 待 push：`348615c`+（含本 checkpoint）
- 全库 `audit_aisha_round_cohorts.py` **勿用**（>60min）；用 `sample20` 或 `c2_batch --audit-round N`
