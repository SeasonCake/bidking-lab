# Handoff 2026-06-13 — 艾莎 gap audit + 批 B

> **主策略文档**：[`docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md`](docs/hero_ref_aisha_strategy_2026-06-13.zh-CN.md) — 后续按该文 C1→C2 顺序推进。

## Git 基线

| 项 | 值 |
|---|---|
| 当前 | **`de2b438`** — 署名 + B2 残差 avg_cells + B3 0052 |
| 下一刀 | **C1** good_regression balanced ±15% pytest |

## 批 B #1 完成（`9982365`）

- `total_grid_target_from_known_high_tier_cells`：curated `total_cells` miss **69%→68%**；exact-total 子集 n=14 **50%**
- pytest：7 条 exact-total band 不回归 + synthetic 金价-only

## 当前焦点 — 批 B #3（主线）

1. ✅ **0052 + 白绿 bridge 回归**：`test_ref_engine_aisha_0052_fatbeans_r3_live_bridge_regression`（r3 combo/balanced + split）
2. **0052 downgrade 路径**：已有 synthetic 单测；不在 n=1 fatbeans 上反复调
3. UI 件/格展示 — working tree，**不进主线 commit**

## 样本纪律（2026-06-13，防过拟合）

**现实**：173 curated / 14 exact-total **不足以**靠 fatbeans  alone 把 MAE 或 miss 压很低；不做合成扩样本的前提下，accept 结构性 miss 会长期存在。

| 做法 | 允许 | 避免 |
|---|---|---|
| 引擎改动 | 有明确语义规则 + synthetic/负例 | 在 14 条上扫 `RESIDUAL×4.0`、≥1/≥2 档阈值 |
| 门禁 pytest | **7 条已 hit** → 不回归；0052/合成 → 路径存在 | 要求 14/14 全 band 才 merge |
| audit | 方向性监控（`audit_aisha_gap.py` 偶尔跑） | 多样本迭代调参直到 miss% 好看 |
| 指标预期 | v0：**少 no-combo、路径可达、报价可用** | v0 追求 curated MAE≈0 |

**批 B #2 调整**：余下 7 条 miss **只跟踪不 optimize**；~~下一刀引擎优先 **有依据的泛化**~~ **已做**：残差格数优先 unfixed tier `avg_cells` 均值，无信号才回退 `4.0`（`total_grid_target_residual_avg_cells_estimate` + synthetic）。

## 批 B #2（低优先级 / 有规则才做）

- `good_regression` balanced ±15% 门禁（仍待做）

## 艾莎格/件推断 — 多策略规划（用户 2026-06-13）

**已落地 ref_v0（B1/B2）**
- 精确总件 + ≥2 档 q3–q5 整数格 → `total_grid_target_from_known_high_tier_cells`
- 残差件 → unfixed tier `avg_cells` 均值，否则 `4.0`

**按轮次（后续 shadow，不在小样本扫参）**

| 轮次 | 信号 | 策略 |
|---|---|---|
| R1 |  mostly 白 | **不用**格子位置/地图似然估具体格（误差大）；count prior + 残差均格 + 默认均格 |
| R2 | 白绿 skill 大致格范围 | 可接地图似然但 band 要宽；用户经验：**仓位误差仍大** |
| R3–R4 | 多档 + 布局更完整 | 地图似然 / 格子位置估件数、格范围较可信 |
| 全局 | 金/红 | 可能在**已知最深格子下方** — 枚举与总格 target 需留 footroom |

**与 v3 关系**：v3 曾用地图/位置似然路线，长期调参未 promotion；ref_v0 若引入同类证据应走 **shadow 约束 / soft target**，与现有枚举叠加，**不**在 173 curated 上迭代权重。

## 本日结论（§61 批 A 完成 → 批 B 启动）

### 样本策略

- **不全库 audit**；244 艾莎 fatbeans → **173 curated**（≥3 轮、有 settlement、去 tail、证据分≥4）
- **代表表 20 条**：[`docs/hero_ref_aisha_representative_samples_2026-06-13.zh-CN.md`](docs/hero_ref_aisha_representative_samples_2026-06-13.zh-CN.md)
- **批 B 回归门禁 15 条**：curated 内 `total_count_exact` 最严重/分层样本（同 doc §批 B）

### 误差排序（v0 应优先）

1. **总格** ~69% miss（exact total 仍 ~67%）— 批 B #1
2. **总件（tier 求和 band）** ~50% miss — 与 prior 未收束同源；用户直觉 **成立**
3. **金/紫格 band** q5 ~49%、q4 ~38%
4. **红值** ~40%；红件 prior 阶段 ~30%，exact total 后 ~0%
5. **balanced vs settlement** ~86% — nest 估价，**非首要修**

### 金价 only

- curated penultimate / R3 均为 **n=0**（抓包窗内金均价常与均格同窗）
- 引擎 today：**必须 avg_value + avg_cells** 才派生金件数
- 批 B #2：total 精确 + 金均价 unique → 试验 `avg_value_only` count pin（手算用户路径）

### q1 100% miss

- 白绿 **split** vs settlement **merged q1** 口径差 — 单独处理，不进批 B 门禁

## 命令

```powershell
cd c:\xiangmuyunxing\biancheng\2026\bidking-lab
python scripts/audit_aisha_gap.py
python scripts/audit_aisha_gap.py --audit-round 3
```
